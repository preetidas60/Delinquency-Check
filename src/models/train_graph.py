# Graph pipeline skeleton (use torch-geometric)
def build_customer_merchant_graph(transactions_df):
    # transactions_df: columns [customer_id, merchant_id, amount, month]
    # implement graph construction: nodes (customers + merchants), edges with weights.
    pass
